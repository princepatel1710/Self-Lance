<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.self_lance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FreelancerDetails"/>
        <activity android:name=".HireAFreelancer" />
        <activity android:name=".LookingForJobDescription" />
        <activity android:name=".Looking_for_job" />
        <activity android:name=".MainActivity" />
        <activity android:name=".forgot_password" />
        <activity android:name=".login"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar.Splash"/>
        <activity android:name=".signup" />
        <activity android:name=".splash"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>